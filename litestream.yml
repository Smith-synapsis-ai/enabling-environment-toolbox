dbs:
  - path: /app/backend/data/agent_store.db
    replicas:
      - type: s3
        bucket: ${LITESTREAM_S3_BUCKET}
        path: agent_store
        region: eu-central-1
        sync-interval: 1s
